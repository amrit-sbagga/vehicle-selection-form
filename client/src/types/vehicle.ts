type VehicleSelection = {
    make: string;
    model: string;
    badge: string;
};

type ServerResponse = {
    make: string;
    model: string;
    badge: string;
    logbook: string;
};


export type { VehicleSelection, ServerResponse, }