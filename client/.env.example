# Copy to client/.env.local and adjust (do not commit .env.local).
#
# Form POST URL. Use a path in dev so Vite proxies to the API (see vite.config.ts).
VITE_API_URL=/api/upload
#
# Backend URL for the Vite dev proxy (vite.config.ts).
VITE_DEV_API_ORIGIN=http://localhost:5000
